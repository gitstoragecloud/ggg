[project]
name = "sqlite-mcp-server"
version = "0.1.0"
description = "A simple MCP server for SQLite database operations"
requires-python = ">=3.10"
dependencies = [
    "mcp>=0.9.0",
]

[project.scripts]
sqlite-mcp-server = "sqlite_mcp_server:main"
